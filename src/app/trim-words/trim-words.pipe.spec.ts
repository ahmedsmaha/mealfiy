import { TrimWordsPipe } from './trim-words.pipe';

describe('TrimWordsPipe', () => {
  it('create an instance', () => {
    const pipe = new TrimWordsPipe();
    expect(pipe).toBeTruthy();
  });
});
